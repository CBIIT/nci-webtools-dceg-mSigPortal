import { groupBy } from 'lodash';
export default function RsInMsigportal(rawData) {
  console.log(rawData);

  var traces;
  var layout;
  var config = {
    //responsive: true,
  };
  return { traces, layout };
}
